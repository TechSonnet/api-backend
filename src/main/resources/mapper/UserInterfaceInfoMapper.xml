<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sonnet.mapper.UserInterfaceInfoMapper">

    <resultMap id="BaseResultMap" type="com.sonnet.model.entity.UserInterfaceInfo">
        <id property="id" column="id" />
        <result property="userId" column="userId" />
        <result property="interfaceInfoId" column="interfaceInfoId" />
        <result property="totalNum" column="totalNum" />
        <result property="leftNum" column="leftNum" />
        <result property="status" column="status" />
        <result property="createTime" column="createTime" />
        <result property="updateTime" column="updateTime" />
        <result property="isDelete" column="isDelete" />
    </resultMap>

    <sql id="Base_Column_List">
        id,userId,interfaceInfoId,totalNum,leftNum,status,
        createTime,updateTime,isDelete
    </sql>
    <update id="updateInvokeCount">
        update user_interface_info
        set totalNum = totalNum + 1,
        leftNum = leftNum - 1
        where id = #{interfaceInfoId}
        and userId = #{userId}
        and leftNum > 0;
    </update>
    <select id="listTopInvokeInterfaceInfos" resultType="com.sonnet.model.vo.InterfaceInfoVO">
        select t2.name as interfaceName, t1.interfaceInfoId, sum(t1.totalNum) as totalNum
        from user_interface_info t1
        join interface_info t2
        on t1.interfaceInfoId = t2.id
        group by t1.interfaceInfoId, t2.name
        order by totalNum desc
        limit 3;
    </select>

</mapper>
